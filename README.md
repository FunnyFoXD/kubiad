# Kubiad

Kubiad is a domain-specific language and compiler for a restricted class of Kubernetes operators.

The manual `WebApplication` operator used as the Kubebuilder baseline is isolated in [`benchmarks/kubebuilder/web-application`](benchmarks/kubebuilder/web-application). Future compiler golden outputs belong in `testdata/codegen`.
