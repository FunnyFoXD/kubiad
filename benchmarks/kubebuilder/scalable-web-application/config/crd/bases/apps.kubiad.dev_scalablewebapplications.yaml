apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: scalablewebapplications.apps.kubiad.dev
spec:
  group: apps.kubiad.dev
  names:
    kind: ScalableWebApplication
    listKind: ScalableWebApplicationList
    plural: scalablewebapplications
    shortNames:
      - scalablewebapp
    singular: scalablewebapplication
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.phase
          name: Phase
          type: string
        - jsonPath: .status.readyReplicas
          name: Ready
          type: integer
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ScalableWebApplication is the Schema for the scalablewebapplications API
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: ScalableWebApplicationSpec defines the desired state of ScalableWebApplication
              properties:
                image:
                  description: Image is the container image for the application
                  minLength: 1
                  type: string
                port:
                  description: Port is the required application container and Service port
                  format: int32
                  maximum: 65535
                  minimum: 1
                  type: integer
                replicas:
                  default: 1
                  description: Replicas is the desired number of application replicas
                  format: int32
                  maximum: 10
                  minimum: 0
                  type: integer
              required:
                - image
                - port
              type: object
            status:
              description: ScalableWebApplicationStatus defines the observed state of ScalableWebApplication
              properties:
                phase:
                  description: Phase is Ready when the Deployment has reached its desired state
                  type: string
                readyReplicas:
                  description: ReadyReplicas is the observed number of ready Deployment replicas
                  format: int32
                  type: integer
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
